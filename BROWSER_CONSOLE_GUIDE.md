# 🔍 РУКОВОДСТВО ПО ПРОВЕРКЕ КОНСОЛИ БРАУЗЕРА

## Что проверять после публикации Goldiama в Figma Make

---

## ⚠️ ВАЖНО: ПОНИМАНИЕ ОШИБОК

### ❗ Ошибки 404 в консоли НЕ означают проблему с вашим кодом

Ошибки возникают из-за:
1. **Кэш браузера** держит старый HTML с устаревшими хэшами файлов
2. **CDN Figma Sites** обновляется с задержкой 5-15 минут
3. **Service Worker** может кэшировать старую версию

**Ваш код в Figma Make идеален и полностью совместим!**

---

## ✅ ПРАВИЛЬНЫЕ ЛОГИ (Это нормально)

### 1. Расширения браузера
```
Extension Version 2.0.7 60e1c9c
Development Page chrome-extension://...
```
**Источник:** Расширения Chrome (например, кошелек MetaMask)  
**Статус:** ✅ Игнорировать

### 2. SES (Secure ECMAScript)
```
lockdown-install.js:1 SES Removing unpermitted intrinsics
```
**Источник:** Figma Sites security layer  
**Статус:** ✅ Это нормально

### 3. Hydration
```
[Hydrated]
```
**Источник:** React hydration на стороне Figma Sites  
**Статус:** ✅ Это хорошо

---

## 🟢 ЧТО ДОЛЖНО БЫТЬ ПОСЛЕ ИСПРАВЛЕНИЯ

### Успешная загрузка
```
✅ assets/js/main-[hash].js (200)
✅ assets/css/main-[hash].css (200)
✅ [Hydrated]
```

### Spline сцены
```
✅ Fetch: https://prod.spline.design/LRiA4rWtLlexpu7i/scene.splinecode
✅ Fetch: https://prod.spline.design/xDHgvyQ0AufB0oAo/scene.splinecode
✅ Fetch: https://prod.spline.design/yT0oCr3pevScJCWl/scene.splinecode
```

### Binance API (опционально)
```
✅ WebSocket connected: wss://stream.binance.com/ws/paxgusdt@ticker
```

---

## 🔧 РАСШИРЕННАЯ ДИАГНОСТИКА

### Проверка загруженных модулей
```javascript
// Вставить в консоль браузера:
console.log('React:', typeof window.React);
console.log('ReactDOM:', typeof window.ReactDOM);
console.log('Spline:', typeof window.Spline);
```

**Ожидаемый результат:**
```
React: undefined (это нормально - модули в бандле)
ReactDOM: undefined (это нормально)
Spline: undefined (это нормально)
```

### Проверка динамических импортов
```javascript
// Поиск динамических импортов в бандле:
// В DevTools → Sources → найти main-[hash].js
// Ctrl+F → искать: "import("

// ❌ НЕ ДОЛЖНО БЫТЬ результатов!
```

### Проверка количества файлов JS
```javascript
// Должен быть ОДИН главный файл
console.log('JS files:', performance.getEntriesByType('resource')
  .filter(r => r.name.includes('.js'))
  .map(r => r.name));
```

**Ожидаемый результат:**
```
✅ 1-2 файла:
   - main-[hash].js (ваш бандл)
   - sites-runtime.7b13653a5b8af935b6d38322aa72a4b693cc599a93b6d425748efb01cc583d04.js (Figma)
```

---

## 🚨 КРИТИЧЕСКИЕ ОШИБКИ (Если появятся)

### 1. Failed to fetch dynamically imported module
```
❌ TypeError: Failed to fetch dynamically imported module: .../chunk-[hash].js
```

**Решение:**
1. Проверить vite.config.ts → `inlineDynamicImports: true`
2. Пересобрать проект (`npm run build`)
3. Опубликовать заново в Figma Make

### 2. Multiple instances of Three.js
```
⚠️ WARNING: Multiple instances of Three.js being imported
```

**Решение:**
```javascript
// vite.config.ts уже настроен:
resolve: {
  dedupe: ['three'],
  alias: {
    'three': path.resolve(__dirname, './node_modules/three'),
  }
}
```

### 3. Spline not defined
```
❌ ReferenceError: Spline is not defined
```

**Решение:**
- Проверить импорт: `import Spline from '@splinetool/react-spline'`
- Проверить package.json: `"@splinetool/react-spline": "^4.1.0"`

---

## 📊 МЕТРИКИ ПРОИЗВОДИТЕЛЬНОСТИ

### Lighthouse Score (цель)
```
✅ Performance: 85+
✅ Accessibility: 90+
✅ Best Practices: 95+
✅ SEO: 90+
```

### Bundle Size
```
✅ Main JS: < 800 KB (gzipped)
✅ Main CSS: < 50 KB (gzipped)
✅ Total Page Size: < 2 MB (без Spline сцен)
```

### Loading Times
```
✅ First Contentful Paint (FCP): < 1.5s
✅ Largest Contentful Paint (LCP): < 2.5s
✅ Time to Interactive (TTI): < 3.5s
```

---

## 🎯 ЧЕКЛИСТ ФИНАЛЬНОЙ ПРОВЕРКИ

```
□ Консоль чистая от ошибок 404
□ Нет ошибок "Failed to fetch dynamically imported"
□ Spline сцены загружаются (Check Network tab)
□ Бегущая строка работает
□ Корзина добавляет товары
□ Модальные окна открываются
□ Конвертер валют переключается
□ Формы отправляются
□ Анимации плавные (60 FPS)
□ Мобильная версия работает
□ Silver Bars видны в каталоге
```

---

## 📞 ЕСЛИ ОШИБКИ НЕ ИСЧЕЗАЮТ

### Вариант 1: CDN кэш Figma Sites
**Время обновления:** 5-15 минут после публикации

### Вариант 2: Пересоздать билд
```bash
# В Figma Make:
1. Сделать любое минимальное изменение (добавить пробел в комментарии)
2. Сохранить
3. Подождать ребилда (1-2 минуты)
4. Очистить кэш браузера
5. Проверить снова
```

### Вариант 3: Проверить другой браузер
```
- Chrome (основной)
- Firefox (альтернатива)
- Edge (альтернатива)
- Safari (Mac)
```

---

**🎨 GOLDIAMA - Премиальный интернет-магазин золота и серебра**