# Checklist для тестирования Figma Make

## 🔄 После применения исправлений

### 1. Очистка кэша
- [ ] Закрыть вкладку Figma Make
- [ ] Открыть DevTools (F12)
- [ ] Network tab → Clear (правый клик → Clear browser cache)
- [ ] Hard Refresh: **Cmd+Shift+R** (Mac) или **Ctrl+Shift+R** (Windows)

### 2. Проверка консоли
Открыть Console и убедиться, что нет:
- [ ] ❌ `404` ошибок для `.js` файлов
- [ ] ❌ `Failed to fetch dynamically imported module`
- [ ] ❌ `Failed to import code layer module`

Допустимые warning'и (можно игнорировать):
- ✅ `Multiple instances of Three.js` (подавлен в коде)
- ✅ Spline buffer warnings (некритично)

### 3. Проверка Network tab
- [ ] Должен загружаться **ОДИН** основной JS файл (например, `index-[hash].js`)
- [ ] НЕ должно быть файлов типа `d7b3ffba...js` или `fb5fb528...js`
- [ ] Проверить, что все assets загружаются (200 OK)

### 4. Функциональное тестирование

#### Hero Section
- [ ] 3D сцена Spline загружается
- [ ] Нет ошибок в консоли при загрузке Spline
- [ ] Ticker Panel работает (цены обновляются)

#### Product Section
- [ ] Карточки товаров отображаются
- [ ] Модалки открываются (ProductDetailModal)
- [ ] Luxury Box Modal работает
- [ ] Корзина добавляет товары
- [ ] Скролл восстанавливается после закрытия модалок

#### Heritage Section
- [ ] AnimatedEagleEmblem отображается
- [ ] Анимация герба работает при смене шага
- [ ] SVG paths загружаются без ошибок

#### Wholesale Section
- [ ] Wholesale баннер отображается
- [ ] Оптовые товары загружаются
- [ ] Модалки работают

#### Compare Block
- [ ] Таблица сравнения отображается
- [ ] Mobile версия переключается корректно

### 5. Performance проверка
- [ ] Lighthouse Score > 70 (Performance)
- [ ] FCP < 2s (First Contentful Paint)
- [ ] LCP < 3s (Largest Contentful Paint)
- [ ] No Long Tasks > 50ms (проверить в Performance tab)

## ❌ Если все еще есть ошибки 404

### Шаг 1: Проверить файлы
```bash
# В консоли DevTools проверить, какой файл не найден
# Пример ошибки: GET https://.../_components/v2/XXXX.js 404

# Это означает, что где-то остался динамический импорт
```

### Шаг 2: Найти источник
1. Открыть Sources tab
2. Поискать файл с ошибкой
3. Посмотреть, откуда он импортируется

### Шаг 3: Исправить
- Заменить `React.lazy()` на прямой импорт
- Убрать `import()` на статический `import`
- Убрать `Suspense` если используется для code splitting

## 🎯 Ожидаемый результат

### Консоль должна быть чистой от:
- ✅ 404 ошибок
- ✅ Failed to fetch module
- ✅ Failed to import code layer

### Network должен показывать:
- ✅ 1 основной JS файл (~2-5 MB)
- ✅ 1 основной CSS файл
- ✅ Все figma:asset изображения (200 OK)

### Функционал должен работать:
- ✅ Все модалки открываются/закрываются
- ✅ Корзина работает
- ✅ Spline сцены загружаются
- ✅ Анимации плавные
- ✅ Скролл восстанавливается

## 📞 Если проблема не решена

1. Проверьте `/FIGMA_MAKE_FIXES.md` - все ли исправления применены
2. Убедитесь, что `vite.config.ts` содержит `inlineDynamicImports: true`
3. Проверьте, что нигде в коде нет `lazy(` или `import(`
4. Попробуйте пересобрать проект в Figma Make

---

**Последнее обновление**: 14 февраля 2026
